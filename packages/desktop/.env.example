# GhostHands Desktop — Environment Configuration
# Copy to .env.staging and .env.production, fill in values for each.

# ─── Supabase (different per environment) ───
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_PUBLISHABLE_KEY="sb_publishable_..."

# ─── GhostHands API ───
GH_API_URL="http://localhost:3100"

# ─── LLM (shared across envs, or override per env) ───
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=

# ─── Debug ───
GH_HEADLESS=false
MAGNITUDE_NARRATE=true
