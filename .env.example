# ─── GhostHands Environment Variables ───────────────────
# Copy this file to .env and fill in real values:
#   cp .env.example .env

# ─── Supabase ───────────────────────────────────────────
# Shared with VALET. Get these from Supabase Dashboard > Settings > API.
# Uses the NEW key format (sb_publishable_... / sb_secret_...).
# Legacy anon/service_role JWT keys are deprecated.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SECRET_KEY=sb_secret_...
SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
# Direct Postgres connection (Settings > Database > Connection string > URI).
# Required by the worker for LISTEN/NOTIFY (bypasses pgbouncer).
SUPABASE_DIRECT_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres

# ─── GhostHands API ────────────────────────────────────
GH_API_PORT=3100
# Service-to-service auth key (shared with VALET backend).
# Generate with: openssl rand -hex 32
GH_SERVICE_SECRET=your-secret-key-min-32-chars
# CORS origin for VALET frontend.
CORS_ORIGIN=http://localhost:3000

# ─── LLM Providers ─────────────────────────────────────
# At least one LLM provider key is required.
# Workers select the provider based on the user's subscription tier.
GOOGLE_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=
# Default model for starter tier (used when OPENAI_API_KEY is set).
GH_DEFAULT_MODEL=gpt-4o-mini

# ─── Security ──────────────────────────────────────────
# AES-256-GCM encryption key for stored credentials.
# Generate with: openssl rand -base64 32
GH_ENCRYPTION_KEY=

# ─── Worker ────────────────────────────────────────────
# Max concurrent browser sessions per worker instance.
MAX_CONCURRENT_JOBS=1
NODE_ENV=development
